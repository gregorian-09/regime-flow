# Backtest Basic

Deterministic backtest on a small CSV dataset. Use this to validate end-to-end engine flow.

## Files

- `config.yaml` - backtest config
- `data/` - sample CSV inputs

## Run

```bash
./build/bin/regimeflow_backtest --config examples/backtest_basic/config.yaml
```
