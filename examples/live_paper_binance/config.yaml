live:
  broker: binance
  paper: true
  broker_asset_class: crypto
  symbols: ["BTCUSDT"]
  broker_config:
    enable_streaming: true
    recv_window_ms: 5000
  reconnect:
    enabled: true
    initial_ms: 500
    max_ms: 10000
  heartbeat:
    enabled: true
    interval_ms: 10000
  risk:
    limits:
      max_notional: 50000
      max_position_pct: 0.2

strategy:
  name: buy_and_hold
  params:
    warmup_bars: 1
